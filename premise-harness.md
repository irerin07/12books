# 전제 하네스

**구현하거나 설명하기 전에** 통과시키는 검사. "이건 이럴 것이다"를 깔고 그 위에 코드나 말을
얹으려 할 때 돌린다.

세 하네스의 자리는 이렇게 갈린다.

| 문서 | 언제 | 무엇을 막나 |
|---|---|---|
| `decision-harness.md` | 무엇을 **고를** 때 | 축이 접히는 것 |
| `source-harness.md` | 무엇이 **사실이라고 말할** 때 | 외부 근거를 지어내는 것 |
| **이 문서** | 무엇을 **만들거나 설명할** 때 | **이 리포가 이미 정해 둔 것을 안 열고 내 짐작을 까는 것** |

`source-harness.md`가 *남의 문서*를 봤다고 말하려면 실제로 봤어야 한다면, 이 문서는 그 안쪽이다 —
**우리 문서를 안 보고 우리 동작을 말하지 않는다.**

## 왜 있는가

전제는 주장처럼 생기지 않아서 검사를 빠져나간다. "서재에 없는 책에 글을 쓰면…"은 근거를
요구하는 문장이 아니라 **배경 설명처럼** 들린다. 그래서 출처 검사에 걸리지 않고, 선택지 검사에도
걸리지 않는다. 그 위에 코드가 얹히고 질문이 만들어진 뒤에야 드러난다.

값이 비싼 이유는 틀린 문장 하나가 아니기 때문이다.

- **잘못된 전제 위에 만든 질문**은 이미 정해진 것을 다시 묻는다. 답하는 사람의 시간을 쓰고,
  답이 돌아오면 그것이 *새로운 결정*으로 굳어 원래 결정과 충돌한다.
- **잘못된 전제 위에 만든 구현**은 테스트가 초록이어도 틀린다. 테스트는 내가 깐 전제를 그대로
  베껴 쓰기 때문이다.
- **잘못된 전제 위에 만든 설명**은 사용자가 자기 제품을 오해하게 만든다. 가장 늦게 드러난다.

이 프로젝트에는 이미 답이 적혀 있는 문서가 있다. `spec.md`는 무엇을 왜 만드는지, `plan.md`는
기술 기반과 공통 규약, 이슈 본문은 그 작업의 범위와 결정권을 적는다. **안 여는 것이 문제지
없는 것이 문제인 적은 없었다.**

## 절차

### 1. 내가 무엇을 깔았는지 찾는다

말하려는 문장이나 쓰려는 코드에서 **"당연히 이럴 것이다"에 해당하는 절**을 꺼낸다. 보통 이런
모양으로 숨는다.

- "지금은 ~하게 동작하니까" — 확인했나, 그럴 것 같은가
- "~는 필요 없을 테니" — 누가 정했나
- "보통 이런 건 ~로 한다" — 이 리포도 그런가
- "A와 같은 이유로 B도" — A의 이유를 실제로 읽었나, 이름이 비슷한가

### 2. 그 전제를 지배하는 것이 무엇인지 정한다

전제마다 **답을 갖고 있는 자리가 정해져 있다.** 짐작할 것이 아니라 열 것이다.

| 전제의 종류 | 지배하는 것 | 예 |
|---|---|---|
| 사용자에게 보이는 동작·정책 | `spec.md` | "책 담기를 잊어도 글은 써진다"(§4.4) |
| 기술 기반·공통 규약 | `plan.md`, `CLAUDE.md` | 커서 페이징, 잠금 순서, 카운터 원자성 |
| 이 작업의 범위·결정권·이미 내린 판단 | **이슈 본문**, `tasks.md` | "기존 정책을 유지하는 동안에는 방치하면 안 된다" |
| 지금 코드가 실제로 하는 일 | 코드와 **테스트** | 조회 경로가 있는가 — `grep`으로 전수 |
| 외부 API의 실제 동작 | `external-apis.md` + 실측 | 문서가 아니라 호출 결과 |

**순서가 있다.** 위에서부터 연다. 아래쪽만 보고 위쪽을 추론하지 않는다 — 코드가 그렇게 되어
있다는 것은 그게 요구사항이라는 뜻도, 아니라는 뜻도 아니다.

### 3. 그 자리를 인용할 수 있는지 본다

열었으면 **줄을 댈 수 있어야 한다.** 파일명과 절 번호, 또는 `file:line`. 못 대면 아직 안 연 것이다.

찾아봤는데 **없으면 그것도 결과다.** "없음"을 확인한 것과 "안 찾아봄"은 다르다. 없으면 그때
비로소 열린 질문이고, `decision-harness.md`로 넘어간다.

### 4. 구현·설명에 전제를 드러낸다

전제를 숨기지 않고 문장에 넣는다. 그래야 틀렸을 때 상대가 잡을 수 있다.

> "`spec.md` §4.4가 자동 생성을 요구사항으로 적고 있습니다 — 그래서 이건 열린 질문이 아닙니다."
>
> "프런트가 이 필드를 쓰는지는 **확인하지 못했습니다**(이 리포에 없음). 백엔드에는 조회
> 경로가 없는 것만 확인했습니다."

## 통과 못 하는 신호

하나라도 걸리면 말하거나 쓰기 전에 연다.

- [ ] 사용자에게 보이는 동작을 설명하려는데 이 세션에서 **`spec.md`를 연 적이 없다**
- [ ] 선택지를 만들면서 `decision-harness.md`의 **"이미 정해져 있다" 칸을 비워 뒀다** — 그 칸은
      추론으로 채우는 칸이 아니라 `spec.md`를 열어야 채워지는 칸이다
- [ ] 이슈를 작업하면서 **이슈 본문을 처음부터 끝까지 읽지 않았다.** 요약만 읽고 시작했다
- [ ] "A와 같은 이유로 B도 이렇게 한다"인데 **A의 이유를 열어 읽지 않았다.** 이름이 닮은 것을
      근거로 삼았다
- [ ] "이 경로는 안 쓰인다"고 말하는데 **전수 검색을 하지 않았다.** 몇 군데 보고 말했다
- [ ] 선택을 먼저 하고 **근거를 나중에 찾았다.** 근거가 선택을 바꿀 수 있었는지 스스로 모른다
- [ ] 확인 못 한 것을 확인한 것과 **같은 어투로** 말했다 (`source-harness.md`의 3등급 문제)

## 사례

### 2026-09-18 · 이미 정해진 것을 정책 질문으로 올렸다

| | |
|---|---|
| 무슨 전제를 깔았나 | 이슈 #50의 "자동 담기의 필요성을 확인한다"를 읽고, **그것이 아직 열린 결정이라고** 전제했다 |
| 무엇이 지배하고 있었나 | `spec.md` §4.4 — "`readingId`는 서버가 (user, book)으로 조회해 자동 연결하고, 없으면 `READING` 상태로 자동 생성한다 — **책 담기를 잊어도 글은 써진다**" |
| 무엇을 했나 | 하네스를 돌렸는데 "결정권: 둘 다 사용자"로 적고 선택지 셋을 만들어 사용자에게 물었다. `decision-harness.md`는 열었지만 `spec.md`는 열지 않았다 |
| 어떻게 드러났나 | 사용자가 "서재에 없는 책에 감상평을 어떻게 만드냐"고 물어서 경로를 확인하다가 §4.4를 읽었다 |
| 왜 그랬나 | **이슈 본문을 요구사항으로 착각했다.** 이슈는 "재검토가 필요하다"는 제안이고, 무엇이 이미 결정인지는 `spec.md`가 답한다. 하네스의 "이미 정해져 있다" 칸을 *추론으로* 비워 뒀다 |
| 무엇이 더 나빴나 | 질문이 거부된 뒤에도 원인을 찾지 않고 **질문을 다듬으려 했다.** 문제는 질문의 모양이 아니라 그 아래 전제였다 |

### 2026-09-18 · 선택을 먼저 하고 근거를 나중에 붙였다

| | |
|---|---|
| 무슨 전제를 깔았나 | 탈퇴 계정의 프로필 수정을 막을 때, **글·댓글과 같은 401이 맞다**고 전제했다 |
| 무엇을 안 봤나 | "행이 하드 삭제됐다면 이 자리에서 무엇이 나왔을까" — `findById`가 비어 **404**다. 401은 행이 남아 있음을 누설한다 |
| 어떻게 드러났나 | 사용자가 "401을 받는다는 건 데이터가 완전히 삭제되지 않았다는 걸 알려주는 꼴"이라고 지적했다 |
| 왜 그랬나 | 기존 코드의 **모양을 따라간 것을 근거로 착각했다.** 세 곳이 401이니 네 번째도 401이라고 보았을 뿐, 그 세 곳이 왜 401인지(탈퇴와 부재가 같은 분기로 끝나서 구별이 안 생긴다)는 나중에야 확인했다 |
| 어떻게 했어야 했나 | 상태 코드를 고르기 **전에** 기준을 세운다 — "탈퇴가 삭제로 보여야 한다면, 하드 삭제의 응답과 같아야 한다" |

### 2026-09-18 · 이슈 본문을 끝까지 안 읽고 "결정 대기"로 묶었다

| | |
|---|---|
| 무슨 전제를 깔았나 | 이슈 #50의 남은 항목이 **전부 정책 결정 대기**라고 전제하고, 오류 하나만 고친 뒤 다른 작업을 제안했다 |
| 무엇이 거기 적혀 있었나 | 2번 항목 끝줄 — "**기존 정책을 유지하는 동안에는 위 오류를 방치하면 안 된다.**" 결정 전에도 고치라는 뜻이다 |
| 어떻게 드러났나 | 사용자가 "이슈 50은 왜 하다 말고"라고 물었다 |
| 왜 그랬나 | 항목의 **분류 표시**(`정책 결정 + 오류 경로`)만 보고 본문을 안 읽었다. 분류는 요약이고 조건은 본문에 있다 |

## 이 문서를 무엇이 강제하나

아무것도 강제하지 않는다. 다른 두 하네스와 같은 이유다 — 기계는 내가 `spec.md`를 열었는지는
알아도, **그 전제가 `spec.md`가 답할 종류였는지**는 판정하지 못한다. 그 판정이 이 검사의 전부다.

대신 **여는 시점이 정해져 있다.** 배경 설명을 쓰려고 할 때, 선택지를 만들 때, 이슈를 집을 때다.
셋 다 "이건 이럴 것이다"가 문장에 들어가는 순간이고, 그때가 이 문서를 여는 순간이다.
