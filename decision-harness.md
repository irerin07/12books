# 결정 하네스

무엇을 고르기 전에 통과시키는 검사. **선택지를 사람에게 내놓기 전**, 그리고 **새 의존·도구·방식을
혼자 정하기 전**에 돌린다. 오래 걸리지 않는다 — 축을 적고, 누가 정하는지 붙이고, 접은 것을 말하면 끝이다.

## 왜 있는가

열린 설계 공간을 객관식으로 바꿀 때 **가장 "결정처럼 보이는" 축만 남고 나머지 축이 암묵적
기본값으로 접힌다.** 접히는 쪽은 보통 "혼자 정할 수 있는 것"이고, 그래서 판단이 미뤄진 게 아니라
**미뤘다는 사실이 잊힌다.** 그 자리를 채우는 기본값은 이 코드베이스의 기준이 아니라 도메인에서
가장 눈에 익은 것 — 벤더가 전면에 내세우는 방식, 튜토리얼에 흔한 것 — 에서 온다.

지적받으면 비용이 싸다. 아무도 안 보면 **접힌 축이 첫 커밋으로 굳는다.**

## 절차

### 1. 축을 먼저 적는다

이 결정에 걸린 질문이 몇 개인가. 하나라고 생각했는데 둘인 경우가 대부분이다.

> 메일 보내기 = ① **누가 보내는가**(발송자: Resend·SES·Postmark)
>             ② **어떻게 말을 거는가**(전송: SMTP·HTTP API)

### 2. 축마다 "누가 정하나"를 붙인다

| 결정권 | 무엇이 여기 오나 |
|---|---|
| **사용자가 정한다** | 계정·비용이 걸린 것, 되돌리기 어려운 것, **사용자에게 보이는 정책** |
| **내가 정한다** | 사용자에게 보이는 정책·보안·운영 부담을 바꾸지 않고, **국소적으로 되돌릴 수 있는** 구현 선택 |
| **이미 정해져 있다** | 규약·기존 코드가 답하는 것 |
| **보류한다** | 지금 정하지 않는다. **왜 미루는지와 무엇을 보면 다시 정하는지**를 함께 적는다 |

**파일 몇 개를 고치느냐가 아니라 결과와 책임이 어디까지 바뀌느냐로 가른다.** 코드로 바꿀 수
있어도 제품 정책인 것이 있다 — "신고 하나를 기각하면 글 전체를 복구할 것인가"는 `restore()`
한 줄이지만, 운영자가 무엇을 누른 것이 되는지를 정하는 정책이다. 그런 것은 내 몫이 아니다.

**보류를 상태로 남기는 이유**는 이 문서가 막으려는 것이 정확히 "미뤘다는 사실이 잊히는 것"이기
때문이다. "부하 측정 후 결정"만 쓰면 다시 볼 일이 없다. **어떤 관측이 나오면 재검토하는지**까지
써야 살아 있는 보류가 된다 — "알림 지연이 p95 5초를 넘으면 큐 도입을 다시 본다"처럼.

### 3. 축 사이의 제약을 확인한다

축을 나눴다고 서로 독립인 것은 아니다. **한 축의 선택이 다른 축의 후보를 제한하면 먼저
밝힌다.** 발송자와 전송 방식은 별개 축이지만, 어떤 발송자는 특정 전송에서만 되는 기능이 있다.
한 번에 한 축만 묻는 원칙은 그대로 두되, **독립적이라는 인상을 주지 않는다.**

### 4. 묻는 축은 한 번에 하나, 선택지는 같은 종류로만

한 목록에 다른 축이 섞이면 **한 축이 다른 축의 성질처럼 보여** 검토에서 통째로 빠진다.

### 5. 내가 정한 축은 말한다

한 줄이면 된다. 이 한 줄이 없으면 나도 그 축을 정했다는 사실을 잊는다.

> "발송자만 정해 주세요 — Resend / AWS SES / Postmark.
> 전송은 SMTP(`JavaMailSender`)로 **제안합니다** — 교체 비용이 낮고 표준 인터페이스입니다.
> 발송자를 바꿔도 환경변수만 바뀝니다. 다른 요구가 있으면 말씀해 주세요."

### 6. 기본값의 근거를 적는다

"그게 흔하다"는 근거가 아니다. 이 프로젝트의 기준은 셋이다 — **교체 비용**, **표준 API인가**,
**새로 생기는 개념 수**. (`plan.md`의 "의도적으로 하지 않는 것"과 같은 잣대다.)

이 셋은 **기능·보안·운영 요구를 이미 충족하는 후보들 사이의 비교 기준**이다. 표준이어도 필요한
기능을 못 하면 애초에 후보가 아니다 — 순서를 뒤집으면 "표준이니까"로 요구사항을 깎게 된다.

### 7. 결정을 짧게 남긴다

다섯 줄이면 된다. 길면 안 쓰고, 안 쓰면 다시 잊는다.

```
축     : 무엇을 각각 결정하는가
결정권 : 사용자 / 나 / 기존 결정
제약   : 축 사이에 불가능하거나 제한되는 조합
결정   : 선택과, 프로젝트 기준에 따른 근거
보류   : 남긴 질문과 다시 볼 시점
```

## 통과 못 하는 신호

하나라도 걸리면 선택지를 다시 만든다.

- [ ] 선택지에 **제품 이름과 방식 이름이 섞여** 있다 — `Resend` · `AWS SES` · `Gmail SMTP`
- [ ] **한 선택지가 다른 선택지를 품는다** — `Resend` · `Resend SMTP`
- [ ] "기타 / 아직 정하지 않음"을 빼고 나면 **공통 축을 말로 설명할 수 없다**
- [ ] 내가 고른 기본값의 근거가 **"그게 흔하다"뿐이다**
- [ ] 질문이 끝난 뒤 **첫 커밋에서 처음 등장하는 개념**이 있다 — 그게 접힌 축이다
- [ ] 내가 정하기로 한 축이 **사용자에게 보이는 결과**를 바꾼다 — 그건 내 몫이 아니다
- [ ] 미룬 축에 **다시 볼 조건이 없다** — 그건 보류가 아니라 망각이다

## 사례

사례가 쌓이는 곳이다. 같은 실수를 또 하면 여기에 줄을 더한다 — 문서가 길어지는 것보다 반복이 비싸다.

### 2026-09-14 · 비밀번호 재설정 메일 발송자

| | |
|---|---|
| 낸 선택지 | `Resend (추천)` · `AWS SES` · `Gmail SMTP` · `아직 정하지 않는다` |
| 무엇이 섞였나 | 앞의 둘은 **발송자**, 셋째는 **전송 방식 + 발송자** |
| 무엇이 빠졌나 | `JavaMailSender`(SMTP). 한 발송자의 성질처럼 보여 후보에 오르지 못했다 |
| 어떻게 드러났나 | 사람이 물어서 — "JavaMailSender를 고려하지 않은 이유가 뭐야" |
| 왜 그랬나 | `plan.md`가 "외부 API 규약이 적용된다"고 적혀 있었고, 이 리포의 유일한 전례가 `KakaoBookClient`(HTTP)라 **새 의존을 있는 틀에 끼워 맞췄다.** 질문을 하나로 줄이려다 전송 축을 접었고, 그 자리에 벤더가 내세우는 HTTP API가 기본값으로 들어왔다 |
| 어떻게 했어야 했나 | **발송자와 전송을 별도 축으로 올려놓고** 시작한다. 전송은 교체 비용·표준 인터페이스·추가 개념 수를 비교해 **내가 제안**하고, 발송자는 계정·비용 조건이 걸리므로 **사용자가 결정**한다 |
| 교훈이 아닌 것 | **"SMTP가 정답"이 아니다.** 이 사례가 남기는 것은 특정 선택이 아니라 *전송도 별도 결정이었다는 사실을 드러내라*이다. 요구가 달라지면 결론도 달라진다 |

## 이 문서를 무엇이 강제하나

**아무것도 강제하지 않는다.** `guard-main.ps1`처럼 훅으로 막는 방법도 만들어 봤지만, 기계가
판정할 수 있는 것은 낱말 수준(`SMTP`·`API` 같은 이름이 일부 선택지에만 있는가)뿐이고 진짜
질문 — *접은 축이 있는가, 그 기본값의 근거가 무엇인가* — 은 사람만 답할 수 있다. 그래서 검사로
위장하지 않고 **읽고 돌리는 목록**으로 둔다.

대신 **선택지를 만드는 순간마다 이 문서를 연다.** 기억해서 지키는 규칙은 지켜지지 않는다.
