# 출처 하네스

**근거를 말하기 전에** 통과시키는 검사. 외부 문서·API 응답·설정 예시·버전·한도처럼 *어딘가에
적혀 있다고 말하는 모든 것*에 적용된다.

**이 리포 안의 것도 같다.** "`spec.md`에 이렇게 적혀 있다", "코드에 그 경로가 없다", "저 제약은
`V8`이 지웠다" — 전부 어딘가에 적혀 있다고 말하는 것이고, 열지 않고 말하면 똑같이 가짜 신호다.
오히려 더 위험하다. 남의 문서는 틀리면 누가 링크를 열어 보지만, **우리 코드에 대한 주장은
말하는 쪽이 제일 잘 안다고 여겨진다.**

`decision-harness.md`가 "무엇을 고를 때"의 검사라면 이것은 "무엇이 사실이라고 말할 때"의
검사다. 그 문서의 1단계(지배 문서를 먼저 연다)는 **결정을 앞둔 경우**를 덮고, 이 문서는
결정과 무관하게 **그냥 설명할 때**까지 덮는다.

## 왜 있는가

기억에서 꺼낸 것을 **인용처럼** 내놓는 일이 있었다. 그럴듯한 형태로 나오기 때문에 읽는 사람은
확인된 것으로 받아들인다. 그게 이 실수의 값이다 — 틀린 정보 하나가 아니라, **확인했다는 신호를
가짜로 붙이는 것**이다. 한 번 그러면 내가 붙인 모든 근거의 값이 같이 떨어진다.

이 프로젝트는 이미 같은 원칙을 외부 API에 대해 적고 있다(`CLAUDE.md`).

> "공식 문서에 그렇게 적혀 있다"는 근거가 아니다 — 카카오 문서는 `page`를 1~50이라 하지만
> 실제로는 500쪽도 200을 준다.

그 규칙은 *문서보다 실측이 낫다*는 말이고, 이 문서는 그보다 한 칸 앞이다 — **문서를 봤다고
말하려면 실제로 봤어야 한다.**

## 말하기 전에 셋 중 어디인지 고른다

| 등급 | 무엇인가 | 어떻게 말하나 |
|---|---|---|
| **확인** | 이 세션에서 열어서 그 자리를 읽었다 | 링크(또는 파일·줄 번호)와 확인 날짜를 붙인다. 붙여넣는 코드·설정은 **그 자리에서 복사한 것만** |
| **요약** | 열긴 했지만 일부만 봤거나 요약했다 | "그 페이지의 A절 기준" 처럼 **범위를 밝힌다.** 안 본 부분으로 확장하지 않는다 |
| **기억** | 안 열었다. 아는 것에서 나왔다 | **"확인하지 않았다"를 문장 안에 넣는다.** 예: "기억으로는 보통 이렇게 씁니다 — 확인은 안 했습니다" |

3등급을 1등급 어투로 말하는 것이 이 문서가 막으려는 전부다.

## 통과 못 하는 신호

하나라도 걸리면 말하기 전에 열어 보거나, 등급을 낮춰 다시 쓴다.

- [ ] **"~에 적혀 있다 / ~에 따르면"** 인데 이 세션에서 그것을 연 적이 없다
- [ ] 남의 문서·API의 **설정이나 응답을 코드블록으로** 붙여넣었는데 출처 링크가 없다
- [ ] **숫자**(한도·타임아웃·버전·요금·상한)를 근거처럼 말했는데 어디서 왔는지 못 댄다
- [ ] "보통 ~이다"를 그 제품에 대한 **사실**로 바꿔 말했다
- [ ] **"없다"고 말했는데 전수 검색을 하지 않았다.** 몇 군데 보고 "안 쓰인다"로 넘어갔다 —
      부재는 전수로만 확인된다. 찾은 범위를 밝히거나(`grep -rn` 한 경로) 등급을 낮춘다
- [ ] 실측이 **뒷받침하는 것**과 문서로 **확인한 것**을 같은 어투로 말했다 — "동작이 그랬다"는
      "메커니즘이 그것이다"가 아니다
- [ ] 정정할 때 **무엇이 틀렸는지 말하지 않고** 다음 작업 얘기로 넘어갔다

마지막 항목이 특히 중요하다. 틀린 것보다 **틀린 것을 작게 만들려는 태도**가 신뢰를 더 깎는다.
정정은 (1) 무엇이 거짓이었는지 (2) 왜 그렇게 나왔는지 (3) 그래서 무엇이 달라지는지 순서로,
상대가 다음 화제로 옮기기 전에 끝낸다.

## 사례

### 2026-09-15 · 없는 인용을 만들어 붙였다

| | |
|---|---|
| 무슨 말을 했나 | `JavaMailSender`가 왜 단독으로 못 쓰이는지 설명하며 "Baeldung 글 앞부분에 이게 있습니다"라고 쓰고 `spring.mail.host=smtp.gmail.com` 설정 블록을 붙였다 |
| 무엇이 거짓이었나 | 그 글을 **열어본 적이 없다.** 설정 블록은 기억에서 나온 것이고, 사용자가 실제 문서의 `smtp.example.com` 스니펫을 보여 주며 드러났다 |
| 왜 그랬나 | 설명이 맞다는 확신이 있었고, **확신을 근거로 착각했다.** 주장을 튼튼하게 보이려고 출처를 붙였는데 그 출처는 확인 대상이 아니라 장식이었다 |
| 더 나빴던 것 | 정정을 두 문단으로 처리하고 곧장 "시작할까요?"로 넘어갔다. 사용자가 "은근슬쩍 넘어가려 하네"로 잡아냈다 |
| 어떻게 했어야 했나 | "기억으로는 Gmail SMTP를 쓰는 예제가 흔합니다 — 그 글은 확인 안 했습니다"로 말하거나, 말하기 전에 실제로 열어 보거나 |

### 2026-09-18 · 실측이 뒷받침한다고 문서를 확인한 것처럼 말했다

| | |
|---|---|
| 무슨 말을 했나 | 동시 기각 수정(PR #55)을 설명하며 — "조건이 맞지 않아 0행이어도 검사한 행의 잠금은 커밋까지 남으므로(REPEATABLE READ — 앱 설정 없이 MySQL 기본값인 것을 **확인했다**)" |
| 무엇이 확인된 것이었나 | **격리 수준뿐이다.** `select @@transaction_isolation`을 돌려 `REPEATABLE-READ`를 봤다 |
| 무엇이 아니었나 | "조건 불일치 행의 잠금이 커밋까지 유지된다"는 **지식에서 나왔다.** MySQL 문서를 열지 않았다. 그런데 괄호 안의 "확인했다"가 문장 전체에 걸리는 것처럼 읽힌다 |
| 실측은 있었나 | 있다 — 10라운드 동시성 테스트가 통과했고, 첫 시도에서 교착이 난 것 자체가 잠금이 잡혔다는 증거다. 하지만 그건 **동작이 그랬다**이지 **메커니즘이 그것이다**가 아니다 |
| 왜 그랬나 | 한 문장 안에 확인한 것(격리 수준)과 확인 안 한 것(잠금 유지)이 섞였고, **확인한 쪽의 괄호가 둘 다를 덮었다.** 거짓을 쓴 것이 아니라 범위를 안 그었다 |
| 어떻게 했어야 했나 | "테스트가 10라운드 통과하는 것으로 직렬화는 확인했습니다. 잠금이 커밋까지 유지되는 메커니즘은 **MySQL 문서를 열어 확인하지는 않았습니다** — 격리 수준이 `REPEATABLE-READ`인 것만 실제로 조회했습니다" |

## 이 문서를 무엇이 강제하나

아무것도 강제하지 않는다. 기계는 내가 어떤 페이지를 실제로 열었는지 알지만, **내가 그것을
근거로 말했는지**는 판정하지 못한다. 그래서 검사로 위장하지 않고 읽고 돌리는 목록으로 둔다
(`decision-harness.md`와 같은 이유).

대신 **근거를 붙이려는 순간이 곧 이 문서를 여는 순간이다.** 출처를 적고 싶어진다는 것 자체가
등급을 고를 때가 됐다는 신호다.

그런데 여는 것만으로는 부족하다 — `decision-harness.md`를 열고도 틀린 적이 있다(그 문서의
2026-09-18 사례). 이 문서가 그보다 나은 자리에 있는 이유는 하나뿐이다. **등급이 문장 안에
들어가서**, 건너뛰면 읽는 사람 눈에 빈자리가 보인다. "확인했다"도 "확인 안 했다"도 없는
사실 주장은 그 자체로 신호다.

그러니 강제는 읽는 쪽이 한다. **등급 없는 사실 주장은 근거를 되물으면 된다.**
